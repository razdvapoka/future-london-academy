@import './consts.styl'
@import './media.styl'
@import './text.styl'
@import './utils.styl'

@font-face
  font-family: OnyFuture
  src: url('../assets/fonts/OnyFuture.woff2') format('woff2'),
  url('../assets/fonts/OnyFuture.woff') format('woff')
  font-weight: normal
  font-style: normal;
  font-display: fallback

html, body
	height: 100%
	width: 100%
	background: $YELLOW
	color: $BLACK

:focus
  outline: none

:root {
	font-family: OnyFuture, 'Helvetica Neue', Helvetica, arial, sans-serif
	font-weight: normal
  font-size: ($ROOT_FONT_SIZE / $CANONIC_SCREEN_WIDTH * 100) vw
}

+mobile()
  :root {
    font-size: ($ROOT_FONT_SIZE / $CANONIC_SCREEN_WIDTH_M * 100) vw
  }

#app
	height: 100%

h2
  font-weight: normal

.input::-moz-placeholder
  color: $GREY
  opacity: 1
.input::-webkit-input-placeholder  /* WebKit, Blink, Edge */
  color: $GREY
.input:-ms-input-placeholder  /* Internet Explorer 10-11 */
  color: $GREY
.input::-ms-input-placeholder  /* Microsoft Edge */
  color: $GREY
.input::placeholder  /* Most modern browsers support this now. */
  padding-top: pxToRem(10)
  color: $GREY
